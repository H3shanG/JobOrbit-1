import{useLocation,useNavigate}from'react-router-dom';import DashboardIcon from'../candidate/DashboardIcon';import styles from'../../pages/AdminDashboard.module.css';const items=[['Dashboard','dashboard','/admin/dashboard'],['Users','people','/admin/users'],['Roles & Permissions','settings','/admin/roles'],['Organizations','briefcase','/admin/organizations'],['Departments','applications','/admin/departments'],['Jobs','jobs','/admin/jobs'],['Applications','applications','/admin/applications'],['Audit Logs','calendar','/admin/audit-logs'],['System Settings','settings','/admin/settings']];export default function AdminSidebar({isOpen,onClose,onLogout}){const location=useLocation(),navigate=useNavigate();return <><button className={`${styles.overlay} ${isOpen?styles.overlayOpen:''}`} onClick={onClose}/><aside className={`${styles.sidebar} ${isOpen?styles.sidebarOpen:''}`}><div className={styles.brand}>◉ JobOrbit</div><nav>{items.map(([label,icon,path])=><button key={path} className={location.pathname===path?styles.active:''} onClick={()=>{navigate(path);onClose()}}><DashboardIcon name={icon}/><span>{label}</span></button>)}</nav><button className={styles.logout} onClick={onLogout}><DashboardIcon name="logout"/>Logout</button></aside></>}
