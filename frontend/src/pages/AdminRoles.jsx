import{useCallback,useEffect,useState}from'react';import{useNavigate}from'react-router-dom';import DashboardLayout from'../components/dashboard/DashboardLayout';import AdminSidebar from'../components/admin/AdminSidebar';import AdminNavbar from'../components/admin/AdminNavbar';import{useAuth}from'../context/AuthContext';import{getAdminRoles}from'../services/adminRoleService';import styles from'./AdminRoles.module.css';
export default function AdminRoles(){const[open,setOpen]=useState(false),[roles,setRoles]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');const{user,logout}=useAuth(),navigate=useNavigate();const load=useCallback(()=>{setLoading(true);setError('');getAdminRoles().then(setRoles).catch(()=>setError('Could not load roles and permissions.')).finally(()=>setLoading(false))},[]);useEffect(load,[load]);return <DashboardLayout sidebar={<AdminSidebar isOpen={open} onClose={()=>setOpen(false)} onLogout={()=>{logout();navigate('/login')}}/>} navbar={<AdminNavbar user={user} onMenuClick={()=>setOpen(true)}/>}><header className={styles.heading}><h1>Roles and Permissions</h1><p>Control the capabilities assigned to each protected system role.</p></header><div className={styles.notice}><b>Security boundary</b><span>System roles cannot be renamed or deleted. Required permissions remain locked to preserve access.</span></div>{loading?<div className={styles.state}>Loading roles…</div>:error?<div className={styles.state}>{error}<button onClick={load}>Retry</button></div>:roles.length===0?<div className={styles.state}>No roles available.</div>:<section className={styles.roles}>{roles.map(role=><article key={role.roleName}><header><h2>{role.displayName}</h2><span>System Role</span></header><p>{role.description}</p><dl><div><dt>Users</dt><dd>{role.userCount}</dd></div><div><dt>Permissions</dt><dd>{role.permissionCount}</dd></div></dl><button onClick={()=>navigate(`/admin/roles/${role.roleName}`)}>Manage Permissions</button></article>)}</section>}</DashboardLayout>}
