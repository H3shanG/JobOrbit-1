import apiClient from'../api/apiClient';export const getAdminDepartments=(params,signal)=>apiClient.get('/admin/departments',{params,signal}).then(x=>x.data);export const getAdminDepartment=(id,signal)=>apiClient.get(`/admin/departments/${id}`,{signal}).then(x=>x.data);export const getDepartmentLookup=(organizationId,includeInactive=false)=>apiClient.get('/admin/departments/lookup',{params:{organizationId,includeInactive}}).then(x=>x.data);export const createAdminDepartment=p=>apiClient.post('/admin/departments',p).then(x=>x.data);export const updateAdminDepartment=(id,p)=>apiClient.put(`/admin/departments/${id}`,p).then(x=>x.data);export const setAdminDepartmentStatus=(id,isActive,reason='')=>apiClient.patch(`/admin/departments/${id}/status`,{isActive,reason}).then(x=>x.data);
